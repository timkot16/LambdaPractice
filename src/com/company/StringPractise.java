package com.company;

import java.util.*;

public class StringPractise {

    public static void main(String[] args) {
        List<String> strings = List.of("first", "abc", "terabc", "ABC", "a2", "a9", "mnb", "last");

//        Stream<String> stringStream = strings.stream().map(String::toUpperCase).distinct();

//        String findFirstOrEmpty = strings.stream().findFirst().orElse("empty");

//        String findLastOrEmpty = strings.stream().skip((long) strings.size() - 1).findFirst().orElse("empty");

//        String a3 = strings.stream().filter(s -> s.equals("a3")).findFirst().orElseThrow();

//        Stream<String> getThirdElement = strings.stream().skip(2L).limit(1);

//        Stream<String> skipAndLimit = strings.stream().skip(1L).limit(2);
    }
}
